// top/bottom
/// Controls where the snackbar appears on screen.
enum SnackBarPosition {
  /// Appears at the bottom of the screen (Flutter default).
  bottom,

  /// Appears at the top of the screen.
  /// Recommended when a bottom sheet or FAB occupies the bottom.
  top,
}
