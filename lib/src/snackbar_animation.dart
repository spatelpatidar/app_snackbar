// slide/fade/none
/// The entrance/exit animation style for the snackbar.
enum SnackBarAnimation {
  /// Slides up from the bottom (or down from the top).
  /// This is the default Flutter behavior.
  slide,

  /// Fades in and out smoothly.
  fade,

  /// No animation — appears and disappears instantly.
  none,
}
