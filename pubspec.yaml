# Flutter 3.16+, pub.dev metadata
name: app_snackbar
description: >-
  A beautiful, fully customizable Flutter SnackBar utility with 4 types,
  action buttons, loading states, queue support, slide/fade animations,
  top/bottom positioning, rootScaffoldMessengerKey support, and global
  Material 3 theme override.
version: 1.0.0
homepage: https://github.com/spatel/app_snackbar
repository: https://github.com/spatel/app_snackbar
issue_tracker: https://github.com/spatel/app_snackbar/issues

topics:
  - snackbar
  - ui
  - notification
  - material

#screenshots:
#  - description: 'AppSnackBar types showcase'
#    path: screenshots/types.png

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
